# GitHub App credentials
# Create at: https://github.com/settings/apps/new
#
# Required permissions (read-only):
#   - Contents: Read
#   - Metadata: Read
#   - Pull requests: Read (optional, for PR stats)
#   - Issues: Read (optional, for issue stats)
#
# Callback URL: {NEXT_PUBLIC_APP_URL}/api/auth/callback
# Setup URL: {NEXT_PUBLIC_APP_URL}/api/auth/callback
# Check "Request user authorization (OAuth) during installation"

GITHUB_APP_ID=your_app_id
GITHUB_APP_SLUG=git-rewind
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"

# OAuth credentials (from GitHub App settings)
GITHUB_CLIENT_ID=your_client_id
GITHUB_CLIENT_SECRET=your_client_secret

# Public URL for OAuth callbacks
NEXT_PUBLIC_APP_URL=http://localhost:3000

# GitHub App slug (for install link)
NEXT_PUBLIC_GITHUB_APP_SLUG=git-rewind

# Session encryption secret (generate with: openssl rand -base64 32)
SESSION_SECRET=your_session_secret_at_least_32_chars
